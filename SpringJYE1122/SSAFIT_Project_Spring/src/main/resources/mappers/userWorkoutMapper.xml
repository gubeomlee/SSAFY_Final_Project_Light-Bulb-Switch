<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.board.model.dao.UserWorkoutDao">

	<select id="sellectAllRelation" resultType="UserWorkout">
		select * 
		from UserWorkouts; 
	</select>

	<select id="sellectWorkoutByUserId" resultType="Workout">

	</select>

</mapper>